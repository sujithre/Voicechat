node_modules/
.env
*.log
.DS_Store
app.zip

# Brand asset: ships with the deployment, but this repo is public.
public/logo.png

# Customer-specific prompts and throwaway probes: both carry material that must
# not be published (engagement content, internal endpoints, project names).
agent-instructions.md
probe-*.mjs
